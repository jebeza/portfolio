import type { Config } from "tailwindcss"

const config: Config = {
  theme: {
    extend: {},
    screens: {
      xs: "480px",
      sm: "640px",
      md: "768px",
      lg: "1290px"
    },
  },
}

export default config
